# اتصال دستگاه تردد و اپ‌های موبایل — معماری

این سند نشان می‌دهد چطور **دستگاه ساعت‌زنی (تشخیص چهره/اثر انگشت)**، **اپ نگهبان**
و **اپ معدن (عکس + موقعیت مکانی)** به سیمرغ‌کارا وصل می‌شوند.

## نقطهٔ اتصال واحد: API دریافت تردد ✅ (ساخته‌شد)

همهٔ کلاینت‌ها (دستگاه، اپ نگهبان، اپ معدن) به یک نقطه تردد می‌فرستند:

```
POST /api/<slug>/attendance/ingest
Authorization: Bearer <device-token>
Content-Type: application/json

{ "email": "ali@co.ir"  |  "member_id": "<uuid>",
  "kind": "in" | "out",
  "at":   "2026-06-25T08:01:00Z",     // اختیاری؛ پیش‌فرض = اکنون
  "photo_url": "https://…",           // اختیاری (چهرهٔ ثبت‌شده)
  "lat": 35.70, "lng": 51.40 }        // اختیاری (موقعیت مکانی)
```

- توکن دستگاه در جدول `attendance_devices` ساخته می‌شود (نوع: `terminal`/`guard`/`mobile`).
- تردد با `source` مناسب (`device`/`guard`/`mobile`) به‌همراه عکس و مختصات ذخیره می‌شود.
- این ترددها **در همان کارنامهٔ حضور** ظاهر می‌شوند و در سقف ثبت دستی شمرده **نمی‌شوند**.

> ساخت توکن دستگاه (فعلاً دستی؛ UI مدیریت دستگاه‌ها فاز بعدی):
> ```sql
> INSERT INTO tenant_<slug>.attendance_devices (name, token, kind)
> VALUES ('ترمینال درب اصلی', '<random-token>', 'terminal');
> ```

## ۱) دستگاه تشخیص چهره / اثر انگشت (ترمینال سخت‌افزاری)

دستگاه‌های رایج (ZKTeco، Suprema، Hikvision و…) دو حالت دارند:

- **Push**: خود دستگاه رویداد را به یک URL می‌فرستد → یک **آداپتور کوچک** (سرویس
  واسط) رویداد را به فرمت بالا تبدیل و به `ingest` POST می‌کند.
- **Pull/SDK**: با SDK دستگاه روی شبکهٔ محلی، یک **سرویس پل (bridge)** هر چند ثانیه
  لاگ تردد را می‌خواند و به `ingest` می‌فرستد.

تطبیق هویت: کد پرسنلی دستگاه ↔ کاربر سیمرغ (با `email` یا `member_id`؛ افزودن
`personnel_code` به اعضا در فاز بعد توصیه می‌شود).

**وضعیت:** API آماده است. سرویس پل/آداپتور بسته به مدل دستگاه نوشته می‌شود (فاز بعد).

## ۲) اپ اندروید نگهبان (تشخیص چهره دمِ درب)

- نگهبان روی گوشی/تبلت، چهرهٔ هر نفر را می‌گیرد.
- تطبیق چهره با **ML Kit Face / یک سرویس تشخیص چهره** → کد کاربر.
- اپ با توکن `kind='guard'` به `ingest` می‌فرستد (`source='guard'` + `photo_url`).
- تحت شبکهٔ شرکت یا اینترنت کار می‌کند (همان API).

**ساخت:** اپ نیتیو جداگانه (Kotlin/Flutter) — فاز بعد. API و مدل داده آماده‌اند.

## ۳) اپ معدن روی گوشی خود کارمند (عکس + موقعیت مکانی)

- کارمند در محل معدن، اپ را باز می‌کند → عکس سلفی + **GPS** می‌گیرد.
- اپ با توکن `kind='mobile'` به `ingest` می‌فرستد (`source='mobile'` + `photo_url` + `lat`/`lng`).
- سرور می‌تواند فاصله تا محدودهٔ مجاز معدن را بسنجد (geofence — فاز بعد).

**ساخت:** اپ نیتیو جداگانه — فاز بعد. API و فیلدهای عکس/مختصات آماده‌اند.

## امنیت

- هر دستگاه/اپ توکن جدا دارد؛ غیرفعال‌سازی با `is_active=false`.
- بهتر است روی HTTPS و در صورت نیاز IP-allowlist اجرا شود.
- `last_seen` برای پایش سلامت دستگاه ثبت می‌شود.

## خلاصهٔ وضعیت

| بخش | وضعیت |
|---|---|
| API دریافت تردد (`/ingest`) + توکن دستگاه + عکس/GPS | ✅ ساخته‌شد |
| نمایش ترددهای دستگاه در کارنامه | ✅ |
| سرویس پل دستگاه سخت‌افزاری | ⬜ فاز بعد (وابسته به مدل) |
| اپ نگهبان (تشخیص چهره) | ⬜ فاز بعد |
| اپ معدن (عکس+موقعیت) | ⬜ فاز بعد |
| UI مدیریت دستگاه‌ها در پنل کارگزینی | ⬜ فاز بعد |
