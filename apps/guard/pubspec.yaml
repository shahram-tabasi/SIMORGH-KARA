name: guard_attendance
description: اپ نگهبان سیمرغ‌کارا — ثبت تردد دمِ درب با تصویر چهره
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.7
  google_mlkit_face_detection: ^0.11.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
